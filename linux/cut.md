## Cut

```bash
cut -f1- -d':' -s --output-delimiter='/' sample.txt
-b：指定范围提取字节
-c：指定范围提取字符
-f：指定范围提取字段
-d：指定分隔符默认是TAB
-s：不包括那些不含分隔符的行(去掉标题和注释)
    如果没有这个参数，则不满足条件的行依然会打印出来
    
范围表示：
N     ## 第N项
N-    ## 第N项到行尾
N-M   ## N到M
-M    ## 行始到M
-     ## 一行所有项
```
